#include<stdio.h>
#include<math.h>

int main()
{
    int N, C, T;
    scanf("%d %d %d", &N, &C, &T);
    int matriz[2][N];
    for(int i=; i < N)

    for(int i = 0; i <3; i++){

    }

    return 0;
}